# T5 editor facade runtime guard

Added `src/lib/editor/editorSurface.runes.test.ts` with three cases: the 226-member public surface, setter descriptors for all 22 externally assigned members, and value round trips with restoration. The real rune-backed singleton is exercised and the focused runes suite passes.

No member required exclusion. Removing a listed facade setter is detected by the descriptor and round-trip guards.
