# T1 modulation hardware parity

Added 17 cases to `src/lib/graphs/modulationGraphs.test.ts`: 13 per-capture circular-shift RMS guards, three superseded-model negative controls, and one mean-RMS guard. The TypeScript scores reproduce the reference measurements and the focused suite passes.

No unassertable behavior or product bug was found. The known saw plateau residual remains explicitly bounded and documented rather than modeled.
