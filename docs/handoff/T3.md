# T3 compressor parity gaps

Added 4 cases to `src/lib/graphs/compressorGraphs.test.ts`: served ratio/Knee sweeps, direct Knee mapping/defaults, ignored-parameter contracts, and measured preset 018 characterization. The focused suite passes with 30 cases.

Preset 018 cannot be claimed as model parity: Auto Makeup was enabled in the recorded capture and is deliberately unmodeled. Its digitized curve is now exercised as a named guard proving the known gap rather than loosening the existing 2px parity tolerance.
