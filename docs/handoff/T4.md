# T4 thin graph invariants

Added 15 derive cases across `adsrGraphs.test.ts`, `megaTapGraphs.test.ts`, `cabAlignmentGraphs.test.ts`, and `cabMicGraphs.test.ts`; `eqGraphs.test.ts` already had 21 cases. Coverage includes absent layouts/live values, unrelated widgets, split rows, enum bindings, stable keys, page isolation, and degenerate specs. All 50 focused cases pass.

Rendered-point finiteness/window and shape monotonicity remain inside `.svelte` components for ADSR, MegaTap, and Cab Alignment. They cannot be unit-asserted without component mounting or production extraction, both prohibited by this milestone.
